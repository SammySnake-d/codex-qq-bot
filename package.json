{
  "name": "codex-qq-bot",
  "version": "1.1.6",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "summary": "node src/group-summary/cli.js run",
    "summary:force": "node src/group-summary/cli.js run --force",
    "deploy": "zsh scripts/deploy.command",
    "ncc": "zsh scripts/ncc.command",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1"
  }
}
